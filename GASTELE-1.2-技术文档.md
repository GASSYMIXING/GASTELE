# GASTELE 1.2 技术文档

> 厂商：Gassymixing  
> 产品：GASTELE  
> 插件版本：1.2  
> 文档日期：2026-09-19  
> 文档依据：当前交付版本的 C++ 源码、插件元数据与本地验证记录。

本文件描述已经实现的功能、算法和限制。公式与参数以 1.2 版代码为准。关于“座机感”“颗粒感”等听感描述，是依据处理结构给出的方向，不代表对特定真实设备进行过测量拟合。

## 1. 产品定位与规格

GASTELE 是电话／通信风格的低保真音频效果器。三个模式使用相同的处理结构，通过固定频段、软饱和、采样保持、量化和输入包络控制噪声形成不同音色。

| 项目 | 当前实现 |
|---|---|
| 格式 | VST3 |
| 平台 | Windows x64 |
| SDK | Steinberg VST3 SDK 3.8.0，标签 `v3.8.0_build_66` |
| 分类 | `Fx\|Distortion` |
| 输入／输出 | 一个音频输入总线、一个音频输出总线；匹配的单声道或立体声 |
| 宿主音频缓冲格式 | 32 位浮点、64 位浮点 |
| DSP 内部运算 | 主要使用 `double` |
| 接受的采样率 | 有限数值，且不低于 8000 Hz；代码没有另设上限 |
| 已进行 DSP 测试的采样率 | 8、22.05、44.1、48、96、192 kHz |
| 额外缓冲延迟 | 0 样本；滤波仍会产生相位变化 |
| 自动化 | 六个声音控制参数可自动化 |
| 状态保存 | 保存六个参数的目标值，支持随宿主工程恢复 |
| 界面 | Win32 原生子窗口，GDI+ 绘制，图片内嵌 |

本版没有实现真实的电话语音编码器、移动通信协议、无线电解调电路或特定电话机的测量模型。模式名称表示音色方向。

## 2. 参数定义

| ID | 宿主参数名称 | 界面控件 | 范围 | 默认值 | 归一化默认值 |
|---|---|---|---|---|---|
| 0 | Bypass | LINE ON / LINE OFF | 0：效果开启；1：旁通 | 0，效果开启 | 0 |
| 1 | Character | CHARACTER | 0–100% | 65% | 0.65 |
| 2 | Style | LANDLINE / MOBILE / RADIO | 三档 | LANDLINE | 0 |
| 3 | Mix | MIX | 0–100% | 100% | 1 |
| 4 | Output | OUTPUT | −24～＋24 dB | 0 dB | 0.5 |
| 5 | Realphone | Realphone 按钮 | 关闭 / 开启 | 关闭 | 0 |
| 100 | Output Level | 底部电平灯条 | 0–1，只读 | 0 | 0 |

注意：宿主参数 `Bypass=1` 对应界面 `LINE OFF`。两者的开／关语义相反。

STYLE 的三个归一化值分别为 `0`、`0.5`、`1`。实际模式索引计算为：

```text
mode = clamp(round(2 × Style_target), 0, 2)
```

## 3. 符号约定

| 符号 | 含义 |
|---|---|
| `Fs` | 宿主采样率，Hz |
| `n` | 采样位置 |
| `C` | 平滑后的 CHARACTER 归一化值，0–1 |
| `M` | 平滑后的 MIX 归一化值，0–1 |
| `B` | 平滑后的 Bypass 归一化值，0–1 |
| `O` | 平滑后的 OUTPUT 归一化值，0–1 |
| `x` | 当前声道的原始输入 |
| `b` | 高通和前置低通处理后的信号 |
| `s` | 软饱和后的信号 |
| `h` | 采样保持值 |
| `q` | 量化后的保持值 |
| `e` | 噪声控制包络 |
| `u` | 后置低通之前的信号 |
| `p` | 三种模式交叉淡化后的湿声 |

除特别注明外，所有音频幅度均使用浮点线性幅度，`1.0` 对应标称数字满刻度。

## 4. 总体信号流

```text
                         ┌──────────── 原声支路 ──────────────────────────────┐
输入 x ──────────────────┤                                                   │
                         └─ 湿声输入限制到 ±16                                │
                            │                                                │
                            ├─ LANDLINE ─┐                                   │
                            ├─ MOBILE ───┼─ 模式权重交叉淡化 ─ 湿声 p ───────┤
                            └─ RADIO ────┘                                   │
                                                                             ▼
                                                        MIX 与 Bypass 控制的线性混合
                                                                             │
                                                        OUTPUT 与 Bypass 控制的增益
                                                                             │
                                                        Realphone 可选串联 EQ
                                                                             │
                                                                           输出
```

每条模式支路的结构一致：

```text
高通 → 前置低通 → tanh 软饱和 ────────────────────┐
                         └→ 采样保持 → 量化 ─────┤
                                                ▼
                                     按 CHARACTER 内部混合
                                                │
                 前置滤波信号 → 包络 → 噪声 ────┤
                                                ▼
                                            后置低通
                                                │
                                            该模式湿声
```

三个模式持续处理各声道输入。模式切换改变输出权重，不会在每次切换时重新启动对应模式的滤波器和采样保持状态。

## 5. 三种模式的精确参数

| 项目 | LANDLINE | MOBILE | RADIO |
|---|---:|---:|---:|
| 高通截止频率 | 320 Hz | 450 Hz | 230 Hz |
| 前置低通截止频率 | 3400 Hz | 2800 Hz | 4300 Hz |
| 后置低通截止频率 | 3400 Hz | 2800 Hz | 4300 Hz |
| 驱动倍数 `D(C)` | `1+5C` | `1+8C` | `1+11C` |
| 保持更新目标速率 `Fh(C)` | `16000−6500C` Hz | `12000−6000C` Hz | `18000−6500C` Hz |
| 量化比例指数 `E(C)` | `14−6C` | `12−7C` | `14−6C` |
| 量化比例 `L(C)` | `2^(14−6C)` | `2^(12−7C)` | `2^(14−6C)` |
| 噪声基础系数 `A` | 0.0015 | 0.0015 | 0.008 |

所有模式共用：

```text
饱和幅度补偿分母 = 1 + 1.25C
数字破损内部混入比例 = C
新增噪声强度随 CHARACTER 的变化 = C²
```

### 5.1 LANDLINE：座机风格

频段集中在中频，削弱低频厚度和高频空气感。相同 CHARACTER 下，驱动倍数低于其他两种模式；量化步长与 RADIO 相同，噪声基础系数与 MOBILE 相同。

这一模式的设计方向是较传统的电话频段与适度破损感。没有实现 μ-law、A-law 或 G.711 编码。

### 5.2 MOBILE：手机风格

具有最高的高通频率、最低的低通频率、最低的保持更新速率以及最粗的量化步长。因此通常更薄、更封闭，数字颗粒感更突出。

没有实现 AMR、GSM、AAC、语音检测、丢包、网络抖动或编码帧损坏。听到的压缩感主要来自软饱和及频段限制。

### 5.3 RADIO：无线电风格

具有最低的高通频率、最高的低通频率和最大的饱和驱动倍数。噪声基础系数是另外两种的 `0.008/0.0015 ≈ 5.33` 倍。

设计方向为较宽频带、较强饱和和更明显的通信噪声。没有实现 AM/FM 调制解调、载波泄漏、调台漂移、信道衰落或静噪开关。

## 6. 滤波器实现

### 6.1 结构与频率限制

每一级均为二阶双二阶滤波器（biquad），`Q=1/√2 ≈ 0.7071`，采用转置直接 II 型状态更新。

实际用于系数计算的截止频率：

```text
fc_effective = min(fc_nominal, 0.42 × Fs)
```

例如 `Fs=8000 Hz` 时，低通截止频率上限为 3360 Hz。因此 LANDLINE 的 3400 Hz 和 RADIO 的 4300 Hz 都会降到 3360 Hz；MOBILE 的 2800 Hz 不变。

CHARACTER 不改变滤波截止频率或 Q 值。同一模式下，旋钮为 0% 时仍有完整的电话频段限制。

### 6.2 系数公式

定义：

```text
ω = 2π × fc_effective / Fs
cω = cos(ω)
α = sin(ω) / √2
a0 = 1 + α
```

高通系数：

```text
b0 = (1+cω) / (2a0)
b1 = −(1+cω) / a0
b2 = b0
a1 = −2cω / a0
a2 = (1−α) / a0
```

低通系数：

```text
b0 = (1−cω) / (2a0)
b1 = (1−cω) / a0
b2 = b0
a1 = −2cω / a0
a2 = (1−α) / a0
```

每个采样的状态更新：

```text
y  = b0 × x + z1
z1 = b1 × x − a1 × y + z2
z2 = b2 × x − a2 × y
```

每次更新后，绝对值小于 `1e−25` 的状态量被设为零。

### 6.3 响应的解释

单个二阶高通的低频渐近衰减斜率约为 12 dB/oct。前后两个二阶低通在小信号线性参考下，高频渐近衰减合计约为 24 dB/oct。

两个低通之间含非线性处理，且两级使用相同 Q。因此不能把整条湿声支路描述成一个标准四阶 Butterworth 低通。

频率表列出的是各级截止参数，不是理想砖墙式通带边界。截止频率之外仍有逐渐衰减的信号，滤波同时引入相位变化。

## 7. 软饱和

前置滤波输出为：

```text
b = LP_pre(HP(x_wet))
```

饱和计算为：

```text
s = tanh(b × D(C)) / (1+1.25C)
```

输入越大或驱动倍数越高，波峰越容易进入弯曲区，波形变化和谐波越明显。幅度补偿分母用于抑制随 CHARACTER 增加的输出幅度，未进行感知响度匹配。

在 `C=0` 时，仍然执行 `s=tanh(b)`。因此 CLEAN 端具有带限和软饱和，不能等同于原声直通。

当前没有独立压缩器、门限、压缩比或压缩器 Attack/Release 参数；也没有对非线性处理进行过采样。

## 8. 采样保持与量化

### 8.1 采样保持

相位累加器的更新步长：

```text
increment = min(1, Fh(C)/Fs)
phase = phase + increment

当 phase >= 1：
    phase = phase − floor(phase)
    h = s
```

初始化时 `phase=1`、`h=0`，第一次处理即会捕获当前饱和值。其余采样沿用上一次捕获的数值。

`Fh` 是目标保持更新速率，实际更新时刻落在宿主采样点上。非整数采样间隔由相位累加器分配；当 `Fh>=Fs` 时，每个宿主采样都会更新。

插件不会更改工程或输出缓冲的采样率。这里的“降采样感”由采样保持实现，并非完整的高质量重采样过程。

| CHARACTER | LANDLINE 目标更新速率 | MOBILE 目标更新速率 | RADIO 目标更新速率 |
|---|---:|---:|---:|
| 0% | 16000 Hz | 12000 Hz | 18000 Hz |
| 65% | 11775 Hz | 8100 Hz | 13775 Hz |
| 100% | 9500 Hz | 6000 Hz | 11500 Hz |

0% 时该支路虽继续运算，但不混入输出。

### 8.2 幅度量化

```text
L = 2^E(C)
q = round(h × L) / L
Δ = 1/L
```

`Δ` 为量化步长。`std::round` 对恰好位于半整数的值采用远离零的取整方式。

CHARACTER 增加时 `L` 下降、台阶变粗。指数允许连续的小数值；当前没有抖动（dither）、固定字长饱和编码或完整 PCM 封装。因此不能将指数值直接作为标准 PCM 位深宣传。

| 模式 | 0% 时步长 | 65% 时步长 | 100% 时步长 |
|---|---:|---:|---:|
| LANDLINE | 1/16384 | 约 0.000911165 | 1/256 |
| MOBILE | 1/4096 | 约 0.005719085 | 1/32 |
| RADIO | 1/16384 | 约 0.000911165 | 1/256 |

### 8.3 内部混合

```text
t = (1−C) × s + C × q
```

CHARACTER 同时改变采样保持速率、量化步长及其混入比例。该内部混合与界面 MIX 独立：MIX 控制原有风格效果与原声的比例，随后再经过 OUTPUT 与可选 Realphone。

没有专门随 `Fh` 变化的抗混叠滤波器。现有前置低通会衰减部分高频，但饱和之后没有另设抗混叠预滤波。采样保持和非线性产生的混叠、台阶与颗粒会成为音色的一部分；后置低通不能消除所有已经折回频带内的混叠。

## 9. 随输入变化的噪声

### 9.1 包络

噪声包络取自前置滤波输出 `b`，位于饱和之前：

```text
e[n] = max(abs(b[n]), 0.9995 × e[n−1])
gate[n] = min(1, 30 × e[n])
```

新峰值立即更新包络，下降时按指数释放。当包络达到 `1/30 ≈ 0.03333`，即约 −29.54 dBFS 的线性幅度时，`gate` 达到 1。

释放系数按每个采样固定，等效时间常数为：

```text
τ_envelope = −1 / (Fs × ln(0.9995)) 秒
```

48 kHz 时约为 41.66 ms；采样率变化时，该毫秒值也变化。

### 9.2 随机序列与幅度

使用 32 位 xorshift 伪随机发生器，移位参数依次为 `13、17、5`，初始种子为 `0x125789ab`：

```text
random ^= random << 13
random ^= random >> 17
random ^= random << 5
r = random / 4294967295 × 2 − 1
noise = r × A × C² × gate
u = t + noise
mode_output = LP_post(u)
```

模式系数 `A` 为 LANDLINE/MOBILE 的 `0.0015`，以及 RADIO 的 `0.008`。

各模式和声道采用同一初始种子；在同步推进时，底层随机序列相同。左右声道包络分别计算，但噪声并非独立去相关的立体声噪声。

### 9.3 静音与噪声电平的边界

从清零状态输入数字静音，包络为零，不产生新增噪声。已经播放声音后转为静音，滤波状态和包络需要衰减，可能存在短暂尾音。

`C=0` 时新增噪声为零。噪声经过后置低通后，其输出峰值与 RMS 不可直接由系数当作实测电平。

## 10. 默认 CHARACTER=65% 的参数快照

此表描述参数平滑稳定后的值。

| 项目 | LANDLINE | MOBILE | RADIO |
|---|---:|---:|---:|
| 驱动倍数 | 4.25 | 6.20 | 8.15 |
| 饱和幅度补偿分母 | 1.8125 | 1.8125 | 1.8125 |
| 目标保持更新速率 | 11775 Hz | 8100 Hz | 13775 Hz |
| 量化比例指数 | 10.1 | 7.45 | 10.1 |
| 量化步长 | 0.000911165 | 0.005719085 | 0.000911165 |
| 内部饱和信号占比 | 35% | 35% | 35% |
| 内部保持／量化信号占比 | 65% | 65% | 65% |
| 包络全开时的噪声幅度系数 | 0.00063375 | 0.00063375 | 0.00338 |

噪声行表示后置滤波之前、随机序列所乘的幅度系数，不是最终输出的噪声 RMS。

## 11. MIX、OUTPUT 与旁通

设模式混合后的湿声为 `p`；Realphone 位于这个混合和输出增益之后：

```text
wet = M × (1−B)
G = 10^((−24+48O)/20)
G_final = G + (1−G) × B
y_pre = [x + (p−x) × wet] × G_final
R = 平滑后的 Realphone 参数
amount = R × (1−B)
y = y_pre + [RealphoneEQ(y_pre)−y_pre] × amount
```

以下前三行描述 Realphone 关闭时的结果；开启时再串联经过 EQ。LINE OFF 始终旁通全部声音处理。

| 稳定状态 | 结果 |
|---|---|
| LINE ON，MIX=100% | 完整湿声，再乘 OUTPUT 增益 |
| LINE ON，MIX=0% | 原声，再乘 OUTPUT 增益 |
| LINE ON，MIX=50% | 原声与湿声各一半，再乘 OUTPUT 增益 |
| LINE OFF | 旁通效果和 OUTPUT，回到原声 |

MIX 是线性混合，没有等功率补偿。干湿信号可能存在相位和幅度差，因此中间位置的响度不一定恒定，也可能出现频率上的相消或增强。

OUTPUT 控制干湿混合后的全部声音。MIX=0% 时若要保持原始音量，OUTPUT 还需设为 0 dB。

没有最终输出限幅器或自动防削波。输出可超过标称 0 dBFS，后续宿主和处理链如何处理取决于它们自身。

## 11A. Realphone 串联 EQ（1.2 新增）

按钮默认关闭。信号顺序为：**原有风格处理 → MIX → OUTPUT → Realphone EQ → 电平表／输出**。Realphone 可与三种风格任意组合，MIX=0% 时也会处理混合后的原声；LINE OFF 则旁通全部效果。它不是第四种互斥风格。

| 频段 | 类型 | 频率 | 增益 | Q | 斜率 |
|---|---|---:|---:|---:|---|
| 1 | 低切／高通 | 523 Hz | — | 0.729 | 24 dB/oct |
| 2 | Bell | 948 Hz | +4.45 dB | 2.923 | — |
| 3 | Bell | 1610 Hz | +6.20 dB | 2.354 | — |
| 4 | Bell | 2654 Hz | +5.85 dB | 4.842 | — |
| 5 | 高切／低通 | 3568 Hz | — | 0.740 | 24 dB/oct |

截图明确显示高切的 3568 Hz、Q=0.740、24 dB/oct。其余数值经用户授权，根据节点和可见黄色总曲线估算拟合，并非恢复了原预设。每个切频段采用两个同频率、同 Q 的二阶滤波器串联，三个 Bell 各一个二阶节，总共每声道 7 节。表中 Q 是本实现的节参数，不能保证与其他 EQ 产品相同数值具有完全相同响应。

使用双精度 RBJ biquad 系数和转置直接 II 型状态更新。设 w=2πf/Fs、α=sin(w)/(2Q)、A=10^(gain/40)，Bell 未归一化系数为 b=[1+αA, −2cos(w), 1−αA]，a=[1+α/A, −2cos(w), 1−α/A]；系数统一除以 a0。切频段采用标准二阶高通／低通系数，详见源码与 [W3C Audio EQ Cookbook](https://www.w3.org/TR/audio-eq-cookbook/)。频率在极低采样率下限制为不超过 0.42×Fs；例如 8 kHz 下高切实际为 3360 Hz。

开启稳定后全部信号依次通过上述 7 节。仅开关过渡使用 12 ms 时间常数平滑原信号与 EQ 输出，以减少突变；关闭期间仍更新 EQ 历史，避免开启时使用过期状态。关闭稳定后完全保留 1.1 的声音。

拟合参考为 48 kHz。用实际 C++ 实现输出的 8192 点单位脉冲计算频响，与截图提取的 348 个可见点比较：RMS 误差约 **0.101 dB**，最大约 **0.417 dB**。这是截图像素范围内的近似误差，受坐标和线宽影响；不是与 Pro-Q 音频输出的精确比对，也不保证其他采样率误差相同。原始对照表见 `verification/measured-curve.csv`。

低切与高切收窄频带，三处中频提升形成鼻音、听筒共振和突出存在感。新增 EQ 是固定线性滤波，不新增失真、噪声或编码器。输出没有自动增益补偿或限幅，提升可能增加峰值；OUTPUT 可用于降低电平。

## 12. 平滑、模式切换与自动化

### 12.1 参数平滑

每个采样计算：

```text
a = 1 − exp(−1/(Fs × 0.012))
current = current + (target−current) × a
```

当目标与当前值之差小于 `1e−9` 时，当前值直接对齐目标。

时间常数为 12 ms，约 36 ms 完成阶跃变化量的 95%，约 55.3 ms 完成 99%。12 ms 不是固定的淡化完成时长。

OUTPUT 在归一化参数域内平滑，再转换为 dB 和线性增益。初始化及重新激活时，当前参数直接设为目标参数。

### 12.2 模式权重

每个模式对应权重 `w_i`：

```text
selected = round(2 × Style_target)
target_weight_i = 选中模式为 1，其余为 0
w_i = w_i + (target_weight_i−w_i) × a
p = Σ(w_i × mode_output_i)
```

实际模式选择使用目标 STYLE 值；音频过渡通过权重平滑完成。稳定状态下只有一个模式权重为 1。

### 12.3 宿主事件

按宿主参数队列提供的采样偏移更新参数目标，再进行上述平滑。代码没有对相邻自动化队列点之间另行进行线性插值。

支持 `numSamples=0` 时刷新参数目标。界面操作发送 `beginEdit / performEdit / endEdit` 手势，便于宿主记录自动化。

## 13. 声道、数值处理与电平显示

### 13.1 声道与状态

支持单声道输入／输出或立体声输入／输出，输入与输出布局必须匹配。左右声道各有三套风格滤波、保持和包络状态，以及独立的 Realphone EQ 状态，不做左右混音或单声道折叠。

重新激活时重置滤波历史、保持状态、包络、随机发生器和峰值包络。工程恢复不保存这些运行中的瞬时状态。

### 13.2 数值边界

- 非有限输入在进入 DSP 时替换为零。
- 湿声支路输入限制在 `−16～＋16`，约相当于标称满刻度以上 24.08 dB 的幅度；普通干声支路不经过这一限制。
- 计算完成后的非有限 double 输出替换为零，再写入宿主对应格式。
- 参数实时输入归一化到 0–1；非有限实时参数值转换为 0。
- 这些措施不等于最终音频限幅器。

样本处理代码中没有显式分配内存、锁等待或文件读写。三种模式持续运算，旁通不会停止所有内部 DSP 运算。

### 13.3 电平灯条

电平取自 Realphone 之后的最终输出。DSP 峰值包络每个采样帧先乘 `0.9995`，再取各输出声道绝对值的最大值。传给宿主的只读电平值限制在 0–1，每个非空音频块末尾发送一次。

界面约每 33 ms 检查参数，显示端另有 `max(宿主电平, 上次显示值×0.85)` 的下降保持。宿主没有转发该只读输出参数时，界面电平可能不能正常更新。

绘制时：

```text
dB = 20 × log10(显示电平)
点亮数量 = clamp(ceil((dB+54)/54 × 12), 0, 12)
```

极小值按 −80 dB 处理。共有 12 格，显示范围约为 −54～0 dBFS。这是衰减峰值显示，不是 RMS、LUFS、真峰值表，也不能用来确认输出是否被限幅。

## 14. 工程状态格式与兼容性

状态使用小端字节序，格式如下：

| 偏移（字节） | 类型 | 内容 |
|---:|---|---|
| 0 | uint32 | 状态格式版本：2 |
| 4 | float64 | Bypass 归一化目标值 |
| 12 | float64 | Character 归一化目标值 |
| 20 | float64 | Style 归一化目标值 |
| 28 | float64 | Mix 归一化目标值 |
| 36 | float64 | Output 归一化目标值 |
| 44 | float64 | Realphone 归一化目标值 |

写入长度为 52 字节。**状态格式版本 2 与产品版本 1.2 是不同概念。**

读取支持格式版本 1（五个值，44 字节）与 2（六个值，52 字节），所有值均须有限且位于 0–1。旧格式恢复时 Realphone 默认关闭。截断、未知格式版本或非法值会被拒绝；全部读取成功后才应用新参数，避免部分更新。电平表、滤波历史、随机数状态、窗口尺寸不写入该数据。

插件识别码保持为：

```text
Processor CID:  74C3E8A1A5934BDCB31F12F047C961D2
Controller CID: D0E4A165F3C7496A9D14B21C832710EF
```

1.2 保持之前版本的组件识别码和既有参数 ID，追加参数 ID 5；支持读取旧状态。旧版插件不能读取新版格式 2 状态。

## 15. 界面与操作

| 项目 | 当前实现 |
|---|---|
| 原始美术尺寸 | 1499 × 1049 |
| 默认窗口尺寸 | 1000 × 700 |
| 宿主协商缩放范围 | 宽度 750–1499，按底图比例计算高度 |
| 普通旋钮拖动 | 上下移动约 240 个窗口坐标单位覆盖归一化范围 0–1 |
| Shift 拖动 | 对应 1600 个单位的精细调节 |
| 普通滚轮 | 每标准滚轮档改变归一化值 0.01 |
| Shift 滚轮 | 每档改变归一化值 0.001 |
| 双击／Home | 恢复默认值 |
| Tab／方向键 | 切换控件／调整当前控件 |

STYLE、Bypass 和 Realphone 使用离散步进。Realphone 位于三种风格下方，粉色为开启、深色为关闭；点击或聚焦后按空格切换。窗口能否由用户拖动缩放，还取决于宿主提供的插件窗口操作。

三个旋钮使用固定圆形旋钮面与绕圆心转动的粉色指针。外圈、投影、人物、数值框及其他底图元素不随旋钮转动。指针角度为：

```text
θ = −135° + 270° × 参数归一化值
dx = sin(θ)
dy = −cos(θ)
```

因此 50% 归一化值指向正上方；OUTPUT 的归一化 50% 对应 0 dB。

## 16. 安装与升级

完整包中双击 `Install-GASTELE.cmd`，脚本会请求 Windows 管理员权限，并将整个插件包安装到系统 VST3 目录。常见位置：

```text
C:\Program Files\Common Files\VST3\GASTELE.vst3
```

安装流程：

1. 验证源插件文件存在。
2. 若已有旧版，检查旧插件二进制能否独占打开；被占用时停止。
3. 将新版本复制到产品数据目录的暂存区，逐文件比较哈希。
4. 将旧版移动到扫描目录之外的备份位置。
5. 将验证后的新版本移动到目标位置。
6. 若提交失败，且目标不存在，尝试恢复原来的插件。

产品数据目录通常为：

```text
C:\ProgramData\Gassymixing\GASTELE\
    Backups\
    InstallStaging\
    install.log
```

首次安装和升级使用同一入口。需要先退出宿主并完整解压安装包；若旧版放在其他自定义扫描目录，需另外处理那个旧副本。

## 17. 验证记录与范围

| 验证类别 | 已记录结果 |
|---|---|
| 1.2 交付二进制的 Steinberg VST3 Validator | 47 项通过，0 项失败 |
| DSP 回归 | 六种采样率、参数变化、频率衰减、有限输出、干声／旁通、静音、声道隔离和模式差异通过 |
| 原生集成 | 参数状态往返、截断状态拒绝、零样本刷新、事件采样位置、只读电平参数通过 |
| 界面交互 | 创建、打开／关闭、重复打开、开关、风格按钮、旋钮拖动、双击复位和缩放通过 |
| Realphone DSP | 六种采样率、三种风格、三种 MIX 设置下，关闭与 1.1 逐样本完全一致，开启符合完整旧链后串联 EQ 的参考结果 |
| 界面隔离 | 默认状态除新增按钮区域外，与 1.1 像素一致；开启预览已核对 |
| 安装流程 | 首次安装、备份升级、占用拦截和模拟提交失败后的恢复通过 |

1.2 新增 Realphone；原有风格算法通过冻结的 1.1 实现进行逐样本回归。安装测试在工作目录的隔离文件夹中执行，没有实际写入系统 VST3 目录，也没有在自动测试中触发 UAC 提示。

这些测试不等于已经在所有商业宿主中验证。当前没有记录特定商业 DAW 工程中的实机试听或主观音质盲测。

## 18. 已知限制与描述边界

1. 三个模式为通信音色设计，没有实现真实语音编码器或具体电话硬件的完整仿真。
2. CHARACTER=0% 仍保留电话滤波与 `tanh` 饱和。
3. 没有独立动态压缩器、自动增益补偿或最终输出限幅器。
4. 没有过采样、完整抗混叠重采样、抖动或标准固定字长 PCM 编码。
5. 保持更新目标速率不改变宿主音频采样率。
6. 固定每采样噪声包络和 DSP 电平衰减系数的毫秒值随采样率变化。
7. 左右声道处理状态独立，但底层噪声序列相关。
8. MIX 为线性混合，干湿相位和音量差可能影响中间位置听感。
9. 电平灯条依赖宿主转发输出参数，不能判断真实峰值或限制输出过载。
10. 当前交付仅为 Windows x64 VST3，没有独立应用、macOS、VST2、AU 或 AAX 版本。

## 19. 源码与证据索引

以下路径以随完整包提供的源码和验证文件为基准：

| 文件 | 作用 |
|---|---|
| `GASTELE-Source/src/Realphone.h` | 7 个二阶节组成的串联 EQ |
| `GASTELE-Source/tests/RealphoneTests.cpp` | 旧版对照、串联顺序、切换、旁通与多采样率回归 |
| `GASTELE-Source/src/Dsp.h` | 滤波、饱和、采样保持、量化、噪声、平滑、干湿混合和电平包络 |
| `GASTELE-Source/src/Plugin.cpp` | 宿主接口、总线、事件处理、参数声明、状态读写和版本字符串 |
| `GASTELE-Source/src/Plugin.h` | 处理器／控制器声明与组件识别码 |
| `GASTELE-Source/src/Panel.h` | 界面渲染、旋钮指针、读数和灯条 |
| `GASTELE-Source/src/Editor.cpp` | 原生窗口、鼠标键盘操作、自动化手势与重绘 |
| `GASTELE-Source/tests/DspTests.cpp` | 声音处理回归测试 |
| `GASTELE-Source/tests/IntegrationTests.cpp` | 实际处理器／控制器及原生界面测试 |
| `GASTELE-Source/tests/PanelRender.cpp` | 使用实际界面代码输出验证图 |
| `GASTELE-Source/installer/` | 一键安装脚本源码 |
| `GASTELE-Source/sdk-revisions.json` | SDK 及子模块的构建版本记录 |
| `verification/vst3-validator.txt` | 官方插件验证结果 |
| `verification/visual-verification.txt` | 旋钮几何、像素隔离及未改动文件检查 |
| `verification/integration-tests.txt` | 原生集成测试结果 |
| `verification/installer-tests.txt` | 隔离环境安装测试结果 |

公式、参数与功能声明应随实际代码版本更新。本文件没有对未来计划功能作已实现声明。
